# Atoms 交互体验记录与 AtomForge 改进

体验日期：2026-09-12。来源：[Atoms 中文官网](https://atoms.dev/zh)、[登录后首页](https://atoms.dev/zh/dashboard)，以及用户授权查看的已有工程师项目。通过浏览器实际点击与查看页面记录，未调用竞品内部接口。

## 实际体验路径

1. 公开首页：打开 Mike 角色介绍，查看职责与合作入口；点击合作入口进入登录页。角色介绍把“这是谁、能帮什么忙、下一步做什么”放在一起。
2. 案例区：点击案例预览，看到居中的应用示例、邻近案例及对应需求与模板入口。没有点击使用模板；这里验证的是案例展示交互，没有验证案例内部功能。
3. 帮助区：点击部署相关问题，答案在当前位置展开，其他页面内容仍保留。
4. 用户登录后：首页包含发现、项目、模板入口；打开构建模式菜单，说明按需显示。账号当时显示积分用完。
5. 打开已有工程师项目：对话在左、应用在右，顶部有分享与发布入口；已有 2048 页面成功显示。没有开始新生成、购买积分或发布内容。
6. 展开已完成的工作步骤：过程说明、文件操作、工具结果与最终总结分层呈现。同类文件操作带有展开更多入口。读取文件按钮可操作，部分历史终端按钮禁用，不能将所有历史工具卡都视作可重放操作。
7. 点击 README 文件操作卡：右侧切换到文件树与文档标签，左侧对话保持可见；文件内容真实展示。截图确认双栏布局及文件标签。没有修改竞品代码。

## 值得借鉴的交互与本次落地

| 观察与收益 | AtomForge 的处理 | 状态 |
| --- | --- | --- |
| 示例展示与需求相邻，用户容易理解输入和成品的关系 | 原创灵感库：任务清单、习惯打卡、支出记录、个人作品集；分类筛选；示例可以操作，需求可以编辑 | 已实现 |
| 模板入口缩短起步路径 | 点击使用需求只填入首页，保留模式选择与最终创建；已有需求可撤销恢复 | 已实现 |
| 案例与个人项目都有明确入口 | 首页快捷导航连接灵感库、我的项目与帮助 | 已实现 |
| 帮助按需展开，减少首次进入的信息量 | 五个折叠问题说明创建、模式、确认、保存和分享 | 已实现 |
| 工作过程和结果分层，文件操作连到真实产物 | 保留现有可折叠执行步骤；工具结果中的真实文件可点击打开右侧代码页；多个文件折叠显示 | 已实现 |
| 完成后提供下一步方向 | 提供手机体验、空状态、新手引导三项建议；点击只填入可编辑需求，不自动执行 | 已实现 |
| 原创延伸：减少编辑中断带来的重复输入 | 首页草稿按账号保存在当前浏览器，创建成功清空；Ctrl/⌘+Enter 快捷创建，兼容输入法组合输入 | 已实现，非声称竞品已验证功能 |
| 角色职责、过程分组、右侧工作区都应保持连续 | 沿用已有职业头像、招呼动画、角色会话、确认卡与右侧标签页 | 已有能力，本次保留 |

本次示例使用原创 React/CSS 图形与交互，不复制 Atoms 角色素材、模板源码或生成提示词。试用数据只保留到关闭示例；提示中的持久化、筛选等完整要求供正式生成使用，试用预览不等于已经生成的项目。

文件链接仅来源于工具返回的结构化文件列表，并与当前项目文件精确匹配；无效、已删除和外部地址不生成链接。入口明确打开当前版本，不冒充历史快照。有未保存文件修改时，先提示保存或放弃，不通过跳转丢弃编辑。

## 后续优先级与边界

- P1：基于实际生成内容提供更具体的下一步建议；目前为可编辑的通用建议。
- P1：工作记录与历史版本精确关联，目前文件联动打开当前版本。
- P2：可搜索的个人模板保存与复用，目前灵感库是四个内置示例。
- P2：角色介绍接入带上下文的主动建议，继续保持实际执行与讨论的区别。

本次已体验登录后的现有项目，不能据此宣称验证了 Atoms 的新项目生成、团队协作全流程、支付或公网部署。账号无剩余积分；未要求购买，未发起付费操作。公开页面对部署能力的介绍属于产品说明，不能替代实际部署验收。AtomForge 的运行与服务能力边界继续以 ROADMAP.md 为准。

## 本地验收

- TypeScript 与 lint 通过。
- 前端自动化检查 12 项通过，包括文件结果两种格式兼容、去重、不可用路径排除，以及已有对话与用量恢复回归。
- Docker 与浏览器验收结果见 ACCEPTANCE.md。
