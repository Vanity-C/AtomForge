# v0.3.8：版本回滚与分享入口

验证日期：2026-09-15。

## 本次行为调整

回滚直接恢复所选快照的文件、入口文件和当前版本号，不新增版本，不删除或改写其他历史快照。例如已有 v1–v5 时回到 v2，版本历史仍为 v1–v5；继续生成或手动保存时创建 v6。旧实现产生的回滚快照继续保留。

保存使用历史最大版本号递增，避免回滚后覆盖或重复已有版本号。回滚接口支持可选的 `expected_version`，前端会提交当前版本；版本发生变化时返回冲突，提醒刷新。原有未传该字段的请求继续兼容，无需数据库迁移。

工作台同步更新版本提示、会话记录和入口文件。回滚后切回已保存源码并清除旧预览和元素选择，再构建恢复后的版本。

“分享”面板只负责公开只读链接的开启、关闭、复制和预览；链接随当前版本更新。源码 ZIP 下载统一保留在顶部“导出”按钮。“发布与部署”继续负责第三方源码发布、公网部署和站内快照。

本次源码发布同时收录此前工作区保留的工程师模式、任务停止、生成恢复和 runner 进程回收相关修复，使 GitHub 跟进此前生产迭代。

## 已执行验证

| 检查 | 结果与边界 |
| --- | --- |
| 后端相关回归 | 163 passed，分三组执行：7 + 72 + 84。隔离 SQLite 和模拟模型，不使用真实用户项目 |
| 前端逻辑回归 | 69 passed |
| TypeScript / ESLint | 通过 |
| 前端生产构建 | 通过，完成 2 个页面预渲染；仍有既有 Monaco 大分块提示 |
| 工作台浏览器回归 | 1440px / 390px 均通过：回滚、刷新后当前版本标记、分享开关与链接、无重复导出入口、真实 ZIP 下载 |
| 浏览器 API 边界 | 接口使用隔离响应；不将其当成生产实例的回滚验证 |

后端新增用例还覆盖重复回滚、切回较新历史版本、恢复新增/删除文件、分享源码同步、后续保存不重号，以及过期版本与跨项目/跨账号请求拒绝。

浏览器回归：先在 `app/frontend` 启动 `pnpm exec vite --host 127.0.0.1 --port 15174 --strictPort`，再运行 `pnpm test:versions`。可用 `WORKSPACE_TEST_ORIGIN` 指定前端地址。脚本使用本机 Edge，无真实账号；截图及 ZIP 输出到 `node_modules/.version-history-check/`。

## 生产部署状态

生产已更新为 `atomforge:0.3.8`，运行源码提交为 `83e43fcbad0e8c0de2127bdd7e09c2e2d594333a`。应用镜像为 `sha256:24ee4c37174a5f0178eef88c0f43d39504d61e54be19be13907366512f6830a6`。现有 runner 的三个运行模块与发布源码一致，保留原健康容器和进程；app、runner 均健康，自动恢复定时器运行正常。

部署前已备份数据库与配套密钥、配置和被替换源码，保留上一版镜像。切换前无活动生成或发布任务，切换前后 8 个账号、15 个项目、14 个历史版本、23 个源码文件、29 条生成记录和 3 条交付记录的数量与内容哈希一致，签名密钥保持不变，SQLite 完整性为 ok；无需数据迁移。

新镜像在无外网、无生产数据挂载、非 root 的隔离容器中通过 7 项版本与账号回归。准备阶段发现并修正解压目录权限问题，重新构建通过后才切换线上；运行容器的 272 个源码和静态文件逐个校验通过。

公网首页、工作台、团队、设置、个人中心、说明页和数据库健康接口均返回 200；前端主资源 `/assets/index-DxrEeJjl.js` 与本次产物哈希一致。原验收账号、既有项目、团队配置、缓存缩略图和 Netlify 配置检查通过。

生产真实浏览器在 1440px 和 390px 下分别验证：v2 回滚到 v1 后历史不增加，刷新后当前版本标记正确，恢复版本构建成功，分享开关与匿名读取正常，分享面板无重复导出入口，顶部导出生成真实 ZIP。下载的两个 ZIP 均包含 9 个文件，完整性通过且源码确为 v1；再次保存生成 v3，过期回滚请求返回 409。仅使用专用验收账号新建的临时项目，完成后全部清理，既有项目列表保持一致；本轮未调用模型生成应用。

私有部署证据保存在服务器及开发工作区的 `.local/release-v0.3.8/`，不提交账号凭据、数据库或配置备份。数据库备份为 `backups/atomforge-20260915T162933Z.tar.gz`，源码与配置回退归档位于上述私有目录，权限均为 0600。
