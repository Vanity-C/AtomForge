# v0.3.0 发布验证

日期：2026-09-13。所有数字为实际执行结果；模拟接口、真实浏览器与生产检查分开记录。

| 检查 | 实际结果 |
| --- | --- |
| 完整后端回归 | 176 passed；临时 SQLite、模拟模型；2 条依赖弃用提示 |
| 前端 TypeScript / ESLint | 通过 |
| 前端逻辑测试 | 67 passed，含会话轮换、账号切换、团队顺序和缩略图队列 |
| Linux runner | 12 passed；无外网、1536 MB 限制容器内运行 Chromium |
| Linux amd64 镜像 | app 与 runner 均构建成功；前端 2 页预渲染完成 |
| 构建提示 | Monaco 大分块提示仍存在，未作为已解决的性能问题 |
| 团队真实回归 | 习惯打卡草稿复验，31 步自测 + 34 步独立 QA，通过后保存 v1；未重新生成源码 |
| 手机与数据 | 1280/375/320 像素下无横向溢出，七日按钮可操作；两个习惯总进度及刷新恢复通过 |
| 交付看板交互 | 桌面/手机的七列、卡片详情、策略修改与结束后只读验证通过 |

此处的习惯应用案例在开发环境执行；不将其当作生产环境的新生成记录。第三方协议 mock 不代表 Gitee/Netlify 真实账号换绑已逐项人工复验。生产发布结果在部署后补记。

---

# v0.1.0 发布验证

本页记录实际执行的检查，不以功能清单替代测试结果。

检查日期：2026-09-12。公开站点：https://newlifezh.top/。

| 检查 | 实际结果 |
| --- | --- |
| 后端 `pytest app/backend/tests -q` | 59 passed；2 条依赖弃用提示，无失败 |
| 前端 TypeScript / ESLint | 通过 |
| 前端逻辑 `node --test scripts/test-*.mjs` | 17 passed |
| runner 在 768 MB、无外网容器内测试 | 7 passed |
| 本机构建 Linux x86_64 发布镜像 | app、runner 均成功；传输 SHA-256 一致 |
| 服务器容器 | app、runner 健康；Caddy 提供 HTTPS |
| TLS | 源站 Let's Encrypt 证书；Cloudflare 完全（严格） |
| 公开路由 | 首页、工作台、个人中心、说明页均返回 200 |
| 私有接口与配置 | 匿名项目接口 401，`.env.docker` 请求 404 |
| 账号 | 新账号注册、用户名登录、邮箱登录均成功 |
| 真实团队生成 | 五角色完成「今日清单」，2 份源码、v1 保存、19 步 QA 检查通过 |
| 发布 | 独立应用及构建产物未登录可访问 |
| 浏览器持久化 | 公网应用新增任务，刷新后任务及剩余计数保留 |
| 容器重建持久化 | 最终镜像替换后，原登录令牌、项目、源码、已完成任务和公开 v1 均仍有效 |
| 手机预览 | 已检查手机宽度布局，完成任务后剩余计数更新为 0 |
| 数据备份 | SQLite online backup + 配套密钥，隔离恢复并执行 integrity_check 通过 |

应用示例：https://newlifezh.top/apps/G0jOQjc79B0Dcax7 。这是该服务器上实际调用模型生成并发布的应用。

服务器为 2 核 / 1 GB 内存，配置额外 2 GB swap，工作台生成并发为 1。本次生成完成后未见 app/runner OOM 或异常重启；这是一条实际流程验证，不是并发容量保证。

## 验证范围

- 后端：账号唯一性、权限隔离、项目生命周期、版本、团队协作、决策卡、生成恢复和集成协议。
- 前端：类型检查、静态检查、会话和文件显示状态逻辑。
- runner：真实构建及浏览器测试协议。
- 部署：HTTPS、未登录入口、真实模型生成、数据持久化、公开发布链接、数据库备份。

## 范围限制

模拟的第三方接口测试不代表真实 GitHub 应用同步、Stripe 支付或 Tavily 服务已验收。当前服务器的单次流程检查也不是容量或负载测试。

