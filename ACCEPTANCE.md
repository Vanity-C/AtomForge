# AtomForge 本轮验收记录

日期：2026-09-12。上一轮记录保存在 [docs/acceptance-2026-09-11.md](docs/acceptance-2026-09-11.md)。

## 自动化检查

| 检查 | 结果与覆盖 |
| --- | --- |
| 后端 pytest | 15 项通过；独立 SQLite，不调用付费模型 |
| runner Node/Chromium | 5 项通过；无外网、1.5 GB 内存、2 CPU、256 进程、移除 capabilities |
| TypeScript | `tsc --noEmit -p tsconfig.app.json` 通过 |
| ESLint | 通过 |
| Docker 构建 | app / runner 镜像构建通过，Compose 两个服务均 healthy |
| 源码导出 | 真实云端笔记工程，npm 安装依赖后 Vite 6.4.3 独立构建通过 |

后端覆盖：工作台鉴权与隔离、版本与分享、增量文件保留、修复失败后再次构建、只保存通过的版本、发布快照不被后续编辑改变、过期版本冲突、独立应用用户与私有/共享数据、成员角色与撤销、多模型选择且不可重复采用、取消与重启中断、预算拒绝新调用、研究报告持久化、GitHub 保留远端目录外文件、Stripe 签名与重放处理。

runner 覆盖：React TypeScript 编译及计数按钮交互、拒绝越界路径和不支持的导入、发现运行异常及断言失败、可视化回写仍保留交互和其他文件、云 SDK 在应用模块初始化之前可用。云接口使用测试替身，真实接口另验。

## 真实模型和浏览器验证

使用配置中的真实 DeepSeek 服务，没有用预置代码代替本轮生成结果。

- 项目 2「Studio 智能体计数器」：规划、生成 App.jsx/styles.css、浏览器点击增加/减少/重置并检查结果，通过后保存 v1。
- 第二轮只改标题：保存 v2，逐字比较确认 styles.css 原样保留。
- Flash 与 V4 Pro 两个真实模型各自产生候选，均通过按钮测试；浏览器选择 V4 Pro 后保存 v3。此前发布的 v2 仍然保持不变。
- 项目 3「云端笔记 · 独立账号与数据库」：真实生成登录/注册、私人笔记 CRUD、会话恢复、AI 助手。
- 本地浏览器在生成应用内注册专用测试账号，添加“容器重建后，这条云端笔记仍然存在。”，真实模型返回笔记整理建议。
- 浏览器选取 h1，将“我的私人笔记”改为“我的云端灵感”，设置紫色；源码回写、构建、创建 v2 成功，笔记与登录状态保留。
- 浏览器发布云端笔记 v2；登录后可查看已保存的笔记。
- SEO 运行检查发现缺少 document.title 和 description，并保存报告；真实模型生成推广文案草稿并记录用量。

## 部署、数据与在线链路

- 两个 Docker 服务经过重建；原有项目 1「番茄任务钟」仍为 v1，没有改动。
- 原登录令牌仍有效，工作台代码、已发布版本、应用账号与数据库笔记仍在。
- 新增表前产生了 SQLite 自动备份；镜像 `/app` 内没有 `.env*` 配置文件。
- runner 仅连接内部构建网络，app 同时连接对外网络；部署后应用 AI 再次真实返回“验证通过”。
- 两个验收项目已给当前用户 `dev@example.test` 添加协作编辑权限，可从原工作台账号查看和修改。
- 当前临时工作台：[打开](https://regular-memorial-jesse-marshall.trycloudflare.com/dashboard)。
- 云端笔记发布链接：[打开](https://regular-memorial-jesse-marshall.trycloudflare.com/apps/qdE0Vw-EsLLKKZ_8)。
- 计数器发布链接：[打开](https://regular-memorial-jesse-marshall.trycloudflare.com/apps/zteeabb-e9JSHzH5)。
- HTTPS 在线健康检查、工作台、发布页及发布数据接口均返回 200；匿名私有项目接口 401，环境文件路径 404。
- 通过 HTTPS 在线应用 API 登录测试账号并读取本地写入的同一条云端笔记成功。
- 在线浏览器已显示实际运行的应用登录页。后续自动化操作多次超时，线上完整表单交互未全部通过浏览器工具验收；本地浏览器完整交互与在线真实 API 链路均已验证。

## 智能体团队验收（2026-09-12）

- 后端测试共 21 项通过。团队新增覆盖五角色上下文交接、独立测试失败后修复、审查连续拒绝时不产生版本、重试保留模式、取消保留已有版本，以及拒绝无效验收协议。
- 前端 TypeScript 检查和 lint 通过。首页团队选择可传入新建项目；工作台呈现角色状态、交接产出和独立验收报告。
- 项目 4 为真实 DeepSeek 团队验收示例：[团队协作计数器](http://localhost:8080/p/4)。数据库记录五次真实调用：`team_product`、`team_design`、`team_architect`、`team_code`、`team_qa`，共 12,141 tokens。
- 产品、设计、架构产出分别交给后续角色；工程师生成 App.jsx/styles.css。开发自测后，测试工程师独立审查并提供 7 步测试，runner 执行通过后保存 v1，`qa.verified=true`。
- 浏览器中点击增加，计数变为 1；工作看板可展开五角色交接成果及实际执行的独立测试。此次无需自动修复；退回修复分支由隔离测试中的失败用例验证。
- Docker 更新后刷新，计数仍为 1，五角色交接文档和验收通过状态恢复；工程师/团队可双向切换。390px 窄屏下页面无横向溢出，五张角色卡片均可访问。

## 用户参与与角色对话验收（2026-09-12）

- 后端 26 项测试通过；最终的确认优先级修订另通过相关协作回归测试。覆盖需求/方案两道暂停、补充意见重新生成确认卡、重复/过期提交、版本冲突、权限隔离、确认后下游使用批准文档、独立角色问答及工具输入/输出配对。
- 增加旧数据库升级回归测试：保留原有数据，补建 `studio_conversations`，升级前自动备份。部署时验证了该升级路径。
- 真实浏览器在独立项目 5 中向设计师询问蓝色方案，得到真实模型答复；修改需求后出现新的确认卡。等待确认期间重建容器，确认卡、用户决定和角色讨论记录均保留。
- 设计师会话可看到产品经理发来的需求交接，以及设计师发给架构师的方案。工具卡展示真实模型调用、增量文件修改、构建与浏览器测试，包含实际结果和耗时。
- 初始请求和后续修改发生冲突时，产品经理以最新修改修订目标、任务和验收标准；工程师和 QA 接收获确认的文档，原始请求保留在历史记录中，不作为竞争性要求继续传入。
- 真实复测项目 5 最终保存 v2：用户最新确认蓝色替代原始紫色要求，QA 明确按蓝色方案审查，独立测试通过。浏览器实际呈现蓝色界面，点击增加后计数为 1；展开测试卡可查看 8 步独立测试输入和 PASS 结果。

### 对话过程折叠（2026-09-12）

- 同一任务中同一角色的连续记录合并显示，带角色头像、时间和步骤数；完成的过程默认收起，当前执行过程默认展开，手动选择在轮询及角色切换后保留。
- 展开显示步骤时间线，工具卡可二次展开，查看真实输入、结果和耗时；工具开始与结果配对计为一步。最终回复、交接对象和独立确认卡保持可见。
- 项目 5 浏览器实测：展开 26 步测试过程与独立构建工具结果，收起后仍显示验收总结；切换设计师会话再返回，过程保持收起，右侧计数器预览保留。
- TypeScript、lint 和 `node --test scripts/test-conversation.mjs` 的 7 项测试通过，覆盖任务/角色边界、分页工具配对、交错记录、最终回复和旧工程师日志。

### 个人中心与账号体验（2026-09-12）

- 注册要求唯一用户名；用户名和邮箱均可登录同一账号。输入用户名会检查占用，注册与资料保存由服务端再次验证，数据库唯一索引处理并发竞争。
- `/account` 支持编辑用户名、绑定邮箱，上传、更换、预览和移除头像，以及取消未保存的修改。保存成功后右上角头像和用户名立即同步；头像在数据库持久化。
- 33 项后端测试通过，包括用户名大小写与格式验证、双入口登录、修改后旧入口失效、原项目归属保留、并发重名拦截、头像格式/大小验证及旧库幂等升级。TypeScript 和 lint 通过。
- 在独立验收账号中通过浏览器完成：重名注册被拒绝、成功注册、上传合成头像及弹窗预览、重名修改被拒绝、保存新用户名/邮箱/头像、刷新保留，并分别用新用户名和邮箱重新登录。窄屏检查无横向溢出。
- Docker 启动日志确认旧库升级前已备份，新增用户名和头像字段。原账号登录仍有效，原有项目可正常读取。
- 角色下拉切换后可只查看该角色收到、发出的记录；390px 窄屏无页面横向溢出。最终前端类型检查、lint 和 Docker 构建通过，app 与 runner 健康。

### 项目隔离与团队交互（2026-09-12）

- 首页每次创建独立项目，进入不同项目时重置会话与任务状态；数据库使用递增编号，避免删除后编号复用导致旧团队记录出现在新项目。删除时清理项目关联记录，保留账号用量统计，并拒绝删除执行中或等待确认的任务。
- 工程师/团队模式仅在创建时选择，服务端拒绝后续模式变更。升级保留已有项目，并根据最近任务固定模式；升级前自动备份。
- 首页、对话及工作看板统一使用 Milo、Luna、Ollie、Neo、Pip 的名字与原创头像。支持点击头像切换角色会话、回复 Markdown 排版与复制、执行过程展开/折叠。
- 后端 38 项测试、对话分组 7 项测试、TypeScript 和 lint 通过，覆盖删除清理、历史孤立记录、并发创建、模式限制和幂等升级。完成摘要补充后，相关流水线 6 项测试再次通过。
- 独立浏览器验收账号从首页连续创建团队项目 6 和工程师项目 7：第二个项目未出现第一个项目的对话，返回首页输入已清空；两个项目均无模式切换控件。团队出现 Milo 的需求确认卡，头像可切换 Luna 会话，实际过程可展开。团队验收任务已停止。
- 工程师项目生成 v1，浏览器点击按钮后文字由“新旅程”变为“出发”；刷新保留项目模式、对话和版本。补充工程师完成事件，后续任务会显示实际变更摘要及已保存版本，避免结束后停留在“正在保存”。

### 确认选项与倒计时（2026-09-12）

- 需求和方案确认提供具体选项、推荐理由及其他想法。新确认卡默认 30 秒后由服务端采用推荐项；继续思考、切换选项、填写补充时暂停，支持重新计时。暂停及截止时间存入 SQLite，未提交草稿保留在当前浏览器。
- 42 项后端测试通过，覆盖真实计时自动决策、恢复过期计时、暂停恢复、重复提交、其他意见修订、非推荐选项传递给工程师和 QA、版本冲突时暂停自动继续。TypeScript、lint 和 7 项对话分组测试通过。
- 浏览器项目 8 实测：模型提供直接重置/二次确认两种方案；继续思考后超过 30 秒仍暂停，刷新与 Docker 重启均保留暂停和草稿。提交其他想法后生成新方案，两次倒计时自动继续，最终 v1 通过独立验收；实际点击加一与重置后显示“已归零”，不出现确认弹窗。

### 用量同步恢复（2026-09-12）

- 复现旧设置页的 `Failed to fetch` 状态；相同账号重新加载可正常取得用量与预算，定位为一次加载失败后不再请求。
- 用量和预算改为自动重试（2 秒、5 秒、15 秒，之后每分钟），恢复联网、页面重新可见时刷新，并提供手动刷新。失败时保留真实缓存数据，不伪造零用量，不显示底层异常或加载错误弹窗。预算后台同步保留未保存编辑。
- 4 项恢复测试通过：短暂失败后恢复、保留已有计数、请求去重与卸载保护、持续断线时限制重试频率。前端类型检查和 lint 通过。
- 独立本地验收代理连续模拟两次用量及预算接口 503：浏览器仅显示友好同步提示，未刷新页面即自动恢复真实用量与预算。验收代理已停止。

### 角色职业形象与招呼交互（2026-09-12）

- 重新绘制五位原创职业角色：Milo 西装与需求清单，Luna 贝雷帽、绘图板与画笔，Ollie 眼镜与结构蓝图，Neo 连帽衫、耳机与代码电脑，Pip 检查工具与验收清单。首页、对话、确认卡和工作看板使用统一形象。
- 悬停、聚焦及点击打开角色说明，显示名字、职责、专属招呼和工具；挥手、点头动画短暂播放，支持减少动态效果设置。角色会话按钮保留原有切换行为。
- TypeScript、lint、Docker 构建通过。浏览器检查五位角色说明，确认动画名称与实际变换生效，键盘切换可查看介绍；390px 窄屏说明卡处于视口内，点击 Neo 后仍正确切换工程师会话。

### 官网交互研究与应用改进（2026-09-12）

- 实际体验 Atoms 公开角色介绍、案例切换、帮助展开；用户登录后，打开模式菜单与已有工程师项目，展开执行步骤，点击 README 操作卡，确认右侧文件树与文档联动。账号无剩余积分，未验证新生成或发布，详见 [体验记录](ATOMS_INTERACTION_REVIEW.md)。
- 原创灵感库浏览器验收：任务新增后待完成数量增加、勾选可完成；生活管理分类仅显示打卡与记账；周三打卡后为 4/7 天；支出 16 元加 8.50 元得到 24.50 元；摄影筛选仅保留对应作品，点击显示详情。
- 示例需求编辑后正确填回首页并聚焦；可撤销为原有需求；刷新后原文恢复。示例试用期间项目数保持不变。折叠帮助可展开，390px 视口的示例弹窗内容无横向溢出。
- 工程师测试项目的下一步建议只填入输入框，未执行新生成；展开执行记录后点击 App.jsx 打开文件标签；多文件入口展开后可打开 App.css。
- 验证未保存内容会阻止日志跨文件跳转；恢复原文后跳转正确显示 CSS，保存按钮保持禁用，未误写代码。验收中修复了切换时旧编辑缓冲残留的问题。
- 独立测试账号使用 Ctrl+Enter 创建新团队项目，项目只出现本次新需求；产品经理确认卡到达后停止任务。返回首页及再次刷新均保持空草稿；原有测试项目未修改。
- 前端 TypeScript、lint 与 12 项前端测试通过；最终 Docker 重建完成，app 和 runner 健康。

### 蛋形角色（2026-09-12）

- 按用户偏好，将五位智能体统一为原创蛋形伙伴，保留职业工具、专属配色、名字、介绍与招呼动画；共享头像组件同步覆盖首页、对话、确认卡与看板。
- TypeScript、lint 通过；Docker app 与 runner 健康。浏览器确认五个蛋形头像显示正常，角色介绍可切换，挥手与摇摆动画仍绑定生效。

### 减少确认与恢复团队开发（2026-09-12）

- 定位项目 10：没有待决策事项也固定暂停两次；架构产出的 `src/App.jsx` 与根入口约定冲突；后续测试使用固定日期或把周一误当今天，失败只显示泛化超时；空项目预览也启动超时计时器。
- 确认卡只为明确要求用户决定、核心范围冲突、必要外部条件或不可逆操作显示；常规偏好由团队选择合理默认值，不再对空问题生成例行确认。新修改会带上本项目历史用户决定；重试在版本未变化时复用已确认交接与未提交代码草稿。
- 根目录入口要求已明确，收到 `src/App.*` 时自动增加根入口转发，保留源文件及相对引用。失败测试返回步骤、选择器、期望与实际文本，并提醒使用今天专用控件。
- 真实重跑额外定位到交接条目数量限制和 JSON 格式偏差；增加合理文档容量、一次交接格式整理及一次 JSON 格式纠正。纠正请求的真实用量照常记录，不截掉第二个 JSON 对象冒充有效输出。
- 回归覆盖无需确认直达开发、必要确认仍阻塞、暂停/恢复、旧决定继承、重试保留草稿、嵌套入口、格式恢复和计量。首轮后端 39 项通过、7 项因 Windows socket 初始化异常未执行；相应模块单独重跑 9 项通过。后续变更对应团队/确认 18 项、studio 9 项、跨轮决定继承 4 项检查均通过。前端类型检查和 lint 通过；runner 的 5 项真实构建/浏览器检查通过。
- 项目 10 真实生成成功：运行 `604e1f535aab42d5b3851d9e25d5045d` 保存 v1，确认卡数量为 0，开发自测和独立 QA 均通过。浏览器实测今日打卡 0→1、切换上一周与回到本周、刷新后仍为 1、取消后恢复 0；验收数据已恢复初始未打卡状态。
- 空测试项目保持打开超过 20 秒后仍显示正常空状态，没有「让 AI 修复」误导入口。Docker app 与 runner 已更新并健康运行。

### 交互测试上限与生成草稿可见性（2026-09-12）

- 项目 11 原运行在修复选择器后提交了 14 步测试，超过 runner 的 12 步硬限制；App.jsx 与 App.css 已保存在运行草稿中，文件页却只展示已提交版本，因此呈现为空。
- runner 支持最多 48 步、60 秒总时限，同一浏览器上下文顺序执行全部步骤；格式与数量问题明确标为测试协议错误。新增 14 步状态连续性及末步失败检查，防止通过截断测试伪造通过。
- 工程师模式在构建前持久化草稿；文件页同时支持工程师和团队的待验收、失败、中断草稿。草稿只读，可切换已保存版本，有手动编辑时不切换来源。验收通过后回到可编辑正式文件；预览、发布、导出仍使用正式版本。
- 后端 studio/team 21 项、前端文件来源/对话 11 项、runner 真实构建与浏览器 7 项测试通过；TypeScript 与 lint 通过。Docker app 和 runner 已重建并健康运行。
- 浏览器确认原失败项目能查看 App.jsx（548 行）与 App.css，带“生成草稿 · 验收未通过”和只读标记。重新执行产生运行 `6dff11dac9994682b787a290355bff17`：16 步开发自测、8 步独立验收均通过，保存 v1，无新增确认卡和失败步骤。
- 浏览器实测新建带标签任务、待办转进行中、标签筛选、刷新保留任务与状态、编辑标题、删除二次确认；测试任务已删除，保留一个“体验”标签。正式文件可编辑，预览正常运行两个文件。

### 团队开发稳定性与恢复体验（2026-09-12）

- 最新失败为项目 11 的“还需要删除标签的功能”：原运行 `f6b5419cb4ee468381c3d6bd8a28da26` 在三次工程师调用中均输出 12,000 token 并截断，未进入构建；旧需求阶段还把“只能添加，不能删除”的缺陷描述误当为禁止删除的约束。
- 支持带唯一精确上下文的局部 edits，顺序校验、原子应用，保留未涉及文件；输出截断时改变输出策略重试一次，不将耗尽的策略再作为代码修复重复三遍。临时网络/服务错误有限重试，鉴权和取消立即结束；每次有用量返回的模型调用仍记录实际用量并检查预算。
- 用户可持续查看当前进度、自动调整、最终完成或需继续状态；完整诊断保留但默认折叠。读取轮询不重叠、短暂断线保留历史并重连；上翻后出现“回到最新进展”，点击后恢复跟随，浏览器已验证。独立 QA 接受完整的最长 48 步流程，末步须断言实际结果；产品经理增加缺陷描述与目标限制的区分规则。
- 恢复运行 `3117076ccf914990a0ec93cd352d50c6` 成功保存项目 11 的 v3：一次工程师调用输出 1,051 token，随后独立 QA 通过，无错误事件、无新增确认卡、无工程师返工。
- 浏览器实测：创建带“临时验证”标签的临时任务并选中筛选；取消标签删除时标签仍在，确认后标签从管理列表、任务卡和选中筛选中同步移除，任务保留，刷新后删除结果持久化。临时任务与标签已清理，用户原有“生活”标签保留。最终工作台显示 v3 已就绪，预览正常。
- 首轮 resilience/studio/team 25 项通过；新增及最终改动对应 resilience/team/agent-collaboration/checkpoint 27 项通过，前端状态/文件/对话 13 项通过，TypeScript 和 lint 通过。最终 Docker app 已重建，app 与 runner 健康。

## 尚需用户配合

| 项目 | 已完成 | 尚缺 |
| --- | --- | --- |
| GitHub | 加密配置、完整工程单向同步、保留远端其他文件的测试 | 真实仓库和 Token，实际同步验收 |
| Stripe | 一次性会话、后端支付记录、真实签名算法与重复回调测试 | 测试账号密钥、价格、Webhook 及公网配置，真实测试支付 |
| 联网研究 | Tavily 搜索接入、有来源报告、配置缺失提示与替身测试 | Tavily Key，实际检索验收 |

配置入口：自己拥有的项目 → 云服务与发布 → 外部服务。验收示例的协作编辑者无权更改所有者的外部凭据。

生产能力边界与剩余差距见 [ROADMAP.md](ROADMAP.md)，启动和操作步骤见 [README.md](README.md)。当前不是对 Atoms 全部商业能力的等价实现。
