# Copy to .env.docker; this file is read only at container startup.
APP_AI_BASE_URL=https://api.deepseek.com
APP_AI_KEY=
AI_HOURLY_LIMIT=30
# Optional. When omitted, a random secret is generated once in the data volume.
# ATOMFORGE_JWT_SECRET=
